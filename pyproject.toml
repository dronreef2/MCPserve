[project]
name = "ai-tools"
version = "0.1.0"
dependencies = [
    "mcp[cli]",
    "openai",
    "requests"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]