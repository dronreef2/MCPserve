# smithery.yaml (ultra-minimal HTTP server test)
name: test-mcp-server

startCommand:
  type: http
  commandFunction:
    |-
    (config) => ({
      command: 'python',
      args: ['-m', 'http.server', config.port.toString()],
      env: {
        PORT: config.port.toString(),
      }
    })
