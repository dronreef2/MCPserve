# smithery.yaml (versão mais simples)
name: test-mcp-server

startCommand:
  type: http
  commandFunction:
    |-
    (config) => ({
      command: 'python',
      args: ['-c', 'print("Servidor iniciado"); import time; time.sleep(30)'],
      env: {
        PORT: config.port.toString(),
      }
    })
