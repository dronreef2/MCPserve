# smithery.yaml (versão de teste MÍNIMA)

name: test-mcp-server

startCommand:
  type: http
  commandFunction:
    |-
    (config) => ({
      command: 'python',
      # O -m smithery.server executa o servidor definido em pyproject.toml
      args: ['-m', 'smithery.server'], 
      env: {
        PORT: config.port.toString(),
      }
    })
