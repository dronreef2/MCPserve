# smithery.yaml (servidor MCP HTTP restaurado)
name: enhanced-mcp-server

startCommand:
  type: http
  commandFunction:
    |-
    (config) => ({
      command: 'python',
      args: ['-m', 'enhanced_mcp_server.core.server'],
      env: {
        PORT: config.port.toString(),
      }
    })
